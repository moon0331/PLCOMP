worword ( worword , worword ) {
    IF worword > worword
    THEN {
        CHAR worword ;
        RETURN worword > worword ; 
    }
    ELSE {
        INT word ;
        RETURN word > worword;
    }
}
