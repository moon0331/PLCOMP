INT testWhile ( left , right , num ) {
    WHILE left > right {
        IF num > right THEN {
            RETURN num > right ;
        }
        ELSE {
            RETURN right > num ;
        }
    }
}
